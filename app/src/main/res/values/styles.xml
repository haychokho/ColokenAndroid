<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="Theme.Coloken" parent="@android:style/Theme.Holo">
        <!-- Customize your theme here. -->
        <item name="actionBarStyle">@style/Theme.Coloken.ActionBar</item>
    </style>

    <style name="Theme.Coloken.ActionBar" parent="@android:style/Widget.Holo.ActionBar.Solid">
        <item name="android:background">#434343</item>
        <item name="android:icon">@android:color/transparent</item>
    </style>

</resources>
